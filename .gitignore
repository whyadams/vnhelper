# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (Supabase keys etc.)
.env
.env.local
.env.*.local

# Tauri updater signing keys — NEVER commit private key
.tauri-keys/

# Rust build output
src-tauri/target/

# Bundled MCP sidecar — ~115MB compiled binary, regenerated locally via
# `pnpm --filter vnhelper-mcp run bundle:win` (or bundle:mac / bundle:linux).
# Required at Tauri build-time but never committed to git.
src-tauri/binaries/vnhelper-mcp
src-tauri/binaries/vnhelper-mcp.exe
packages/vnhelper-mcp/dist/vnhelper-mcp
packages/vnhelper-mcp/dist/vnhelper-mcp.exe

# Local Claude Code agent caches
.agents/
.claude/
skills-lock.json
memory/

# Stray Ren'Py debug dumps from local testing — never part of the build.
_dbgfile.rpy
*.dbg.rpy

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
